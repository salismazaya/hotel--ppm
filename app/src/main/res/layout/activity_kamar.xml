<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <!-- TOOLBAR -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#121212"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Daftar Kamar"
            android:textColor="#D4AF37"
            android:textSize="18sp"
            android:textStyle="bold"
            android:layout_margin="16dp"/>
    </androidx.appcompat.widget.Toolbar>

    <!-- CONTENT -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="0dp"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/toolbar"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <!-- STANDARD -->
            <include layout="@layout/item_kamar_standard"/>

            <!-- DELUXE -->
            <include layout="@layout/item_kamar_deluxe"/>

            <!-- SUPERIOR -->
            <include layout="@layout/item_kamar_superior"/>

            <!-- SUITE -->
            <include layout="@layout/item_kamar_suite"/>

            <!-- TWIN -->
            <include layout="@layout/item_kamar_twin"/>

            <!-- PRESIDENTIAL -->
            <include layout="@layout/item_kamar_presidential"/>

        </LinearLayout>
    </androidx.core.widget.NestedScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
